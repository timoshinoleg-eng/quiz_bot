# MAX-Квиз Bot Requirements (MVP - упрощённая версия)
# Python 3.10+

# Bot Framework
maxapi>=0.9.15

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# УДАЛЕНО для MVP:
# - asyncpg (только PostgreSQL, для MVP используем SQLite)
# - alembic (миграции не нужны для MVP)
# - aioredis/redis (не нужен для одиночного режима)
# - aiohttp/aiohttp-socks (не используется)
# - pillow (генерация изображений не нужна)
# - python-json-logger/orjson (не критично)
